apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello
  # You'll usually want to add your resources to the argocd namespace.
  namespace: argocd
spec:
  # Source of the application manifests
  source:
    targetRevision: develop
    path: overlays/staging/with-canary
