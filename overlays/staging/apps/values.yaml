spec:
  source:
    api:
      images:
        - api=nginx
    auth:
      images:
        - auth=nginx
    centralConfig:
      images:
        - central-config=nginx
    producer:
      images:
        - producer=nginx
    ui:
      images: []
  destination:
    server: https://kubernetes.default.svc
