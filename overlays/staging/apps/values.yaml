spec:
  source:
    api:
      images:
        - api=nginx
    auth:
      images:
        - auth=nginx
    centralConfig:
      images:
        - central-config=nginx:stable-perl
    ui:
      images:
        - ui=nginx
  destination:
    server: https://kubernetes.default.svc
