spec:
  source:
    api:
      images:
        - api=nginx
    auth:
      images:
        - auth=nginx:stable-perl
    centralConfig:
      images:
        - central-config=nginx
    producer:
      images:
        - producer=nginx
    ui:
      images:
        - ui=nginx
  destination:
    server: https://kubernetes.default.svc
