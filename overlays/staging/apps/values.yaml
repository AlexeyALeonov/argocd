spec:
  source:
    api:
      images:
        - api=nginx
    auth:
      images:
        - auth=nginx
    centralConfig:
      images:
        - central-config=nginx
    producer:
      images:
        - producer=nginx
    ui:
      images:
        - ui=nginx:stable-perl
  destination:
    server: https://kubernetes.default.svc
