spec:
  source:
    api:
      images: []
    auth:
      images:
        - auth=nginx
    centralConfig:
      images:
        - central-config=nginx
    producer:
      images:
        - producer=nginx
    ui:
      images:
        - ui=nginx
  destination:
    server: https://kubernetes.default.svc
