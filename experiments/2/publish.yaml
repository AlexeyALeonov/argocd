apiVersion: v1
kind: Service
metadata:
  name: rollout-analysis-step-service
spec:
  type: ClusterIP
  selector:
    app: rollout-analysis-step
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: rollout-analysis-step
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: rollout-analysis-step-service
              servicePort: 80
      host: experiments.local
