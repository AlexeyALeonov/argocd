apiVersion: v1
kind: Service
metadata:
  name: rollout-secret-service
spec:
  type: ClusterIP
  selector:
    app: rollout-secret
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: rollout-secret
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: rollout-secret-service
              servicePort: 80
      host: experiments.local
