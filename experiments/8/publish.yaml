apiVersion: v1
kind: Service
metadata:
  name: rollout-rollingupdate-service
spec:
  type: ClusterIP
  selector:
    app: rollout-rollingupdate
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: rollout-rollingupdate
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: rollout-rollingupdate-service
              servicePort: 80
      host: experiments.local
