apiVersion: v1
kind: Service
metadata:
  name: rollout-canary-service
spec:
  type: ClusterIP
  selector:
    app: rollout-canary
  ports:
  - port: 80
    targetPort: 8080
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: rollout-canary
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: rollout-canary-service
              servicePort: 80
      host: experiments.local
