apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: hello
resources:
- api.yaml
- configMap.yaml
- namespace.yaml
- sealedsecret_databases.json
vars:
- fieldref:
    fieldPath: metadata.name
  name: API_LOCATION
  objref:
    apiVersion: v1
    kind: Service
    name: api
configMapGenerator:
- name: deployments
configurations:
- config.yaml
